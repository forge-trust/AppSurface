title: AppSurface package chooser
summary: Start with the package that matches the app you are building, then add optional web modules and support surfaces only when you need them.
page_type: guide
nav_group: Start Here
order: 50
breadcrumbs:
  - Start Here
  - Packages
trust:
  status: Package chooser
  summary: This page is generated from evaluated project metadata plus a curated manifest of what first-time AppSurface adopters should install directly.
  freshness: Regenerated in CI whenever the package surface or chooser guidance changes on main.
  change_scope: Repository-wide. The chooser covers AppSurface's coordinated package family, support packages, and proof-host surfaces.
  migration:
    label: Read the pre-1.0 upgrade policy
    href: /docs/releases/upgrade-policy
  archive: Package READMEs remain the deep reference. This chooser is the public install map for the current repo state.
  sources:
    - packages/package-index.yml
    - README.md
    - releases/README.md
