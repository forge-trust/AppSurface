<!-- /autoplan restore point: /Users/andrew/.gstack/projects/forge-trust-AppSurface/main-autoplan-restore-20260906-154049.md -->
# Design: Durable Executable-Contract Adoption Rail

Generated by `/office-hours` on 2026-09-06
Branch: `main`
Repo: `forge-trust/AppSurface`
Status: APPROVED
Mode: Builder
Supersedes: `andrew-main-design-20260901-020115-issue790-tailwind-host-scoped-cli.md` for office-hours branch lineage only; the issue #790 product decision remains independent.

## Problem Statement

A review of seven AppSurface Durable worker lanes in the downstream Skoolit application found that the core protocol is being consumed safely but the adoption layer asks every host to reconstruct too many decisions.

The safe parts should stay unchanged:

- PostgreSQL remains authoritative for discovery, claims, leases, fences, effect permits, completion, recovery, and history.
- Durable acceptance can share the caller's exact `NpgsqlTransaction` with application state.
- Storage registration is passive. Continuously live workers opt into `AddWorkerHost()`, while externally activated workers call the same bounded `IDurableRuntimePump.RunOnceAsync` primitive.
- Wake requests are payload-free latency hints. The database remains the source of truth.
- Provider-keyed work and reconcile-before-retry work preserve their existing safety contracts.
- Application-owned routes, authorization, activation outboxes, lane selection, reconciliation ordering, and provider policy remain outside AppSurface.

The review found these upstream concerns:

1. Every Skoolit lane treats `SchemaCompatible && EpochCompatible` as its main HTTP success decision. That can return success while the runtime is `NotStarted`, `Stale`, or `Draining`, even though AppSurface documents `Healthy` as the only ready state.
2. Scale-to-zero rollout needs a compatibility gate before activation is enabled. Requiring a current worker heartbeat at that stage would create a startup deadlock: activation is disabled, so no pump can establish the heartbeat required to enable activation.
3. Seven lanes repeat Work registration facts when they construct requests. `DurableWorkRequest` currently accepts nine arguments, including identity, version, encoded payload, provider safety, retry policy, and due time.
4. At least 26 downstream tests directly construct Durable health, pump, registration, and host objects because no first-party Durable testing package exists.
5. `runtime_heartbeat` is keyed by worker identity and has no bounded cleanup path. Process-plus-GUID identities are valid, so normal deployments can accumulate stale rows indefinitely.
6. A standalone worker can emit AppSurface activities without exporting them if its host does not install an OpenTelemetry listener, provider, and exporter.
7. A downstream lane used the pump's `TimeBudget` as a hard cancellation deadline around materialization, enqueue, pump, and reconciliation. The contract actually bounds discovery and the start of additional pump items; it does not promise a hard stop for already-started provider work.
8. Seven private external-activation routes repeat compatible orchestration. The repetition may justify an ASP.NET Core adapter, but only if the adapter remains transport-neutral and does not absorb Cloud Tasks, OIDC, application outboxes, lane policy, or domain recovery behavior.

The program should turn those facts into one supported adoption promise:

> Define the Work once, map the host once, and prove the lifecycle once.

## What Makes This Cool

The useful outcome is not a shorter constructor by itself. One immutable Work definition becomes the source for registration, request creation, test setup, safe diagnostics, and later inspection tooling. A developer should be able to see explicit Work identity, codec, retry, and provider-safety facts without repeating them at each call site or replacing them with runtime method serialization.

The host path becomes equally legible:

```text
store/schema/epoch compatible
        |
        +--> activation may be enabled
        |
        +--> pump heartbeat current and state Healthy
                 |
                 +--> runtime is ready
```

An externally activated host can remain at zero instances until a payload-free wake hint arrives, execute the same bounded pump used by a hosted worker, export the expected activities, and test the whole path without inventing a second state machine.

## Constraints

- Preserve the public-preview protocol and its explicit Work name, version, codec, retry, lease, provider-safety, scope, command, and idempotency facts.
- Do not use reflection, assembly scanning, expression-tree method serialization, or execution of a consumer assembly to discover Work.
- Do not make a notification, HTTP route, broker receipt, or application outbox authoritative for durable execution.
- Do not add a default anonymous management endpoint or a Durable-owned operator control plane.
- Do not make `AddAppSurfaceDurablePostgreSql(...)` start a worker, open a connection, or apply migrations.
- Do not infer that an effect was absent from an exception, timeout, or cancellation. Issue #783 owns explicit typed Work exits; issue #765 owns persisted retry plans and execution deadlines.
- Do not expose connection strings, credentials, payloads, scope IDs, aggregate IDs, trace context, provider responses, or arbitrary exception text through health, diagnostics, manifests, or CLI output.
- Keep liveness host-owned. A Durable store query must not become the reason an orchestrator restarts an otherwise functioning process during a dependency outage.
- Keep application route names, HTTP status policy, authorization policy, materialization, continuation, reconciliation order, lane selection, kill switches, and provider rehearsal outside Durable core.
- Keep every implementation case independently reviewable with full public/internal API documentation, failure-path tests, packed-consumer proof, formatting, and affected solution verification.
- A generated contract manifest may land only if it is derived from the same typed definition without a second hand-maintained identity source and without loading consumer code into the CLI.

## Premises

1. Compatibility for activation and runtime readiness are different decisions. A configured runtime can be compatible before it has started, but it is ready only while its state is `Healthy`.
2. The safest first public change is canonical interpretation of existing health facts, not an AppSurface-owned HTTP endpoint.
3. Repeated identity, version, codec, provider-safety, and default retry facts belong in one immutable typed definition. Per-request scope, command, idempotency key, payload, optional due time, and explicit policy override remain request facts.
4. Testing support must exercise public contracts and real provider composition. It must not implement a fake durable state machine that disagrees with PostgreSQL.
5. Heartbeat cleanup is provider-owned maintenance. Runtime callers should not receive direct table `DELETE` privileges.
6. OpenTelemetry emission and host export activation are separate responsibilities. AppSurface should make correct activation copyable and prove it end to end.
7. A transport-neutral external-activation service is the stable low-level host seam proved by the reference host. An experimental `ForgeTrust.AppSurface.Durable.AspNetCore` adapter may reduce HTTP mapping around it; adapter stabilization requires a second distinct host or adopter and a complete failure matrix.
8. A provider-neutral wake-dispatch interface is premature with only one Cloud Tasks-shaped adopter. Record it as a future evidence gate, not an implementation case.
9. The existing `ForgeTrust.AppSurface.Workers` projection contract and `ForgeTrust.AppSurface.Flow.DurableTask` adapter remain separate products. Adoption docs must distinguish them from native AppSurface Durable.

## Landscape Check

The external convention check supports the health split without changing AppSurface's architecture:

- [ASP.NET Core health checks](https://learn.microsoft.com/en-us/aspnet/core/host-and-deploy/health-checks?view=aspnetcore-10.0) and [.NET Aspire health checks](https://learn.microsoft.com/en-us/dotnet/aspire/fundamentals/health-checks) distinguish readiness from liveness and let hosts select checks by endpoint.
- [Kubernetes probes](https://kubernetes.io/docs/concepts/workloads/pods/probes/) give startup, readiness, and liveness different operational consequences. A wrong mapping can either admit traffic too early or restart a process during a recoverable dependency failure.
- [Quartz ASP.NET Core integration](https://www.quartz-scheduler.net/documentation/quartz-4.x/packages/aspnet-core-integration.html) separates scheduler hosting from health registration, matching AppSurface's passive provider registration.
- [MassTransit testing](https://masstransit.io/documentation/concepts/testing) demonstrates the value of a first-party asynchronous test harness. Its separate overall and inactivity timeouts are also a useful warning against reusing one budget for every phase.
- [MassTransit observability](https://masstransit.io/documentation/configuration/observability) demonstrates copyable `AddSource(...)` and exporter activation for both ASP.NET Core and console hosts.

The first-principles conclusion is narrower than the common framework pattern. AppSurface already exposes the required facts. It should genericise the correct decisions and proof tools while leaving HTTP and deployment consequences with the host.

## Cross-Model Perspective

The independent Codex cold read proposed a Durable "executable contract kit": one `DurableWorkDefinition<TWork, TResult>` feeding registration, request creation, compatibility expectations, tests, telemetry names, diagnostics, optional host mapping, and later CLI/template tooling.

Its strongest formulation was:

> Define the work once, map the host once, prove the lifecycle once.

It recommended MassTransit as the developer-experience benchmark for typed registration, health, instrumentation, testing, and examples, while retaining AppSurface's explicit persisted identity, version, codec, schema, epoch, fence, permit, and recovery model.

It also challenged the original decision to wait for a proven ASP.NET adapter contract before building an adapter. That premise was revised: an explicitly experimental adapter will be used to prove the contract after health interpretation, typed definitions, and the initial testing package exist. Public stabilization remains gated on a second distinct host or adopter.

## Approaches Considered

- **Safety patch train:** rejected as the final program because it fixes health interpretation, heartbeat retention, and documentation but leaves registration, testing, host orchestration, and first-run developer experience fragmented.
- **Staged executable-contract adoption rail:** selected because it completes the adoption path through bounded, dependency-ordered cases and keeps each public commitment reviewable.
- **Host-first proving ground:** retained as a technique inside the selected rail through the reference host and experimental adapter, but rejected as the program order because already-proven health and definition improvements should not wait for the experiment.

## Recommended Approach

Create one tracking case and nine independently shippable child cases, grouped into three tracks. The tracking case owns only the user outcome, boundaries, measurement harness/report, dependencies, and deliberate deferrals. It contains no runtime or package implementation.

- **Track A — operational correctness and storage:** cases 1 and 4. These may ship without the rest of the rail.
- **Track B — contract and proof:** cases 2, 3, and 6. These establish the stable developer path.
- **Track C — adoption tooling:** cases 5, 7, 8, and 9. Cases 5 and 7 are experimental/evidence-gated; cases 8 and 9 consume only accepted stable/public-preview contracts. None blocks Tracks A or B.

The tracking case is a roadmap, not a promise to ship all nine children in one release train. Track A remains independently
valuable. Track B is the minimum competitive wedge: one typed Work, one registration, one authorized activation, and one
deterministic lifecycle proof. Track C children become implementation commitments only when their named dependency and
evidence gates are met. A child that fails its evidence gate is closed with the evidence and decision rather than kept alive
as indefinite preview surface.

### Dependency order

| Order | Case | Depends on | May run in parallel with |
| ---: | --- | --- | --- |
| 0 | Executable-contract adoption tracking case | Existing Durable runtime | None |
| 1 | Canonical operational assessments | None | 4 and #783 |
| 2 | Typed immutable Work definitions | #783 | 1 and 4 |
| 3 | Durable testing package | 1 and 2 | 4 |
| 4 | PostgreSQL heartbeat retention | None | 1, 2, and 3 |
| 5 | Configured-target equivalence experiment | A second parser consumer and a completed normalization proof | 6 and 7 |
| 6 | External-activation service and reference host | 1, 2, and 3 | 4 and 5 |
| 7 | Experimental ASP.NET Core activation adapter | 1, 2, 3, and 6 | 4, 5, and 8 |
| 8 | Non-mutating Durable runtime doctor | 1 and 4 | 5, 6, and 7 |
| 9 | Direct-mapping external-worker template | 1, 2, 3, and 6 | 7 and 8 |

Issue #783 is a hard dependency for case 2 and must settle the exit-aware executor shape before case 2 freezes its binding API. Case 2 then ships legacy and exit-aware bindings together over one definition type. Issue #765 remains compatible future policy and does not block this rail.

### Program invariants

1. A definition is configuration, not durable authority. Acceptance still snapshots exact request facts into PostgreSQL.
2. A scenario is proof, not a simulated provider. Provider-sensitive assertions run against the real PostgreSQL implementation.
3. An external activator is a latency path, not a queue-backed source of truth.
4. A doctor is non-mutating by default and never loads or executes application assemblies.
5. A template teaches the stable path only. Experimental APIs do not enter a template until their graduation gate passes.
6. Definitions carry only today's registration and request defaults. They carry no #765 stored attempt plan or execution deadline and no #783 terminal-exit value.
7. Activation outcomes report only what the host adapter observes. They never infer provider-effect truth, recovery state, or a typed Work exit.

### Normative external-activation flow

Cases 3, 6, 7, and 9 use the same phases and outcomes:

| Phase | Input/condition | Required behavior | Observable outcome |
| --- | --- | --- | --- |
| Health precheck | `Unavailable` | Do not call the pump | `Unavailable` |
| Health precheck | Schema or epoch incompatible | Do not call the pump | `Incompatible` |
| Health precheck | `Draining` | Do not call the pump | `Draining` |
| Health precheck | Compatible `NotStarted`, `Healthy`, or `Stale` | The host may attempt one bounded pump; provider admission remains authoritative | Continue |
| Request admission | Request canceled before pump invocation | Do not call the pump | `CanceledBeforeAdmission` |
| Request admission | Host request budget expires before pump invocation | Do not call the pump | `RequestBudgetExceeded` |
| Provider admission | `IDurableRuntimePumpAdmission` refuses an overlapping, draining, or conflicting pass | Return without treating work as processed | `Busy` |
| Pump | Pump returns its aggregate result, including `HasMore` or failed/suspended counts | Preserve the aggregate result exactly | `Completed` |
| Pump | Pump throws or observes cancellation after invocation starts | Do not infer effect or recovery truth | `PumpFailed` with a bounded safe code, or code-free `PumpCanceled` |

`DurableRuntimePumpRequest.TimeBudget` controls discovery and the start of additional items; it is not the host request deadline. Application materialization, continuation, reconciliation, and cleanup execute outside this generic flow and own their own host-defined cancellation tokens. A host must consult the existing durable record and, after #783, typed exits for provider-effect or recovery truth.

Case 6 owns one transport-neutral public-preview contract used by the direct reference endpoint, the stable direct template, and case 7's ASP.NET adapter:

```csharp
public sealed record DurableExternalActivationRequest(
    DurableRuntimePumpRequest PumpRequest,
    TimeSpan RequestBudget);

public enum DurableExternalActivationOutcomeKind
{
    Unavailable = 0,
    Incompatible = 1,
    Draining = 2,
    Busy = 3,
    CanceledBeforeAdmission = 4,
    RequestBudgetExceeded = 5,
    Completed = 6,
    ActivationFailed = 7,
    PumpCanceled = 8,
    PumpFailed = 9,
}

public sealed record DurableExternalActivationResult(
    DurableExternalActivationOutcomeKind Kind,
    DurableRuntimeHealthState? ObservedHealthState,
    string? ProblemCode,
    DurableRuntimePumpResult? PumpResult);

public interface IDurableExternalActivationService
{
    ValueTask<DurableExternalActivationResult> ActivateAsync(
        DurableExternalActivationRequest request,
        CancellationToken callerCancellation = default);
}
```

The service depends only on `IDurableRuntimeHealth`, `IDurableRuntimePumpAdmission`, `TimeProvider`, the canonical AppSurface
`ActivitySource`, and logging. It creates a separate budget token, links it to the caller token, and records an atomic
`pumpStarted` phase immediately before calling the admission-aware pump. Cancellation classification is deterministic:
caller cancellation wins if both sources are canceled before `pumpStarted`; otherwise budget cancellation before that point
is `RequestBudgetExceeded`; after that point either or both sources produce `PumpCanceled`; an
`OperationCanceledException` with neither source canceled is `ActivationFailed` before `pumpStarted` and
`PumpFailed` after it, both with `ASDUR407`.

`PumpResult` is required only for `Completed`; `ObservedHealthState` is present only after a successful health read.
`ProblemCode` accepts only `ASDUR103`, `ASDUR108`, `ASDUR400`–`ASDUR405`, or new `ASDUR407`. It is required for
`Unavailable`, `Incompatible`, `ActivationFailed`, and `PumpFailed`, and may preserve `ASDUR404`/`ASDUR405` from a
stale health observation on another outcome. Constructor validation rejects every contradictory combination.

| Source observation | Activation result |
| --- | --- |
| Health `Unavailable` | `Unavailable` + `ASDUR103` |
| Health `Incompatible` | `Incompatible` + observed `ASDUR108` or `ASDUR400`–`ASDUR403` |
| Health `Draining` | `Draining` |
| Stale health | Continue to provider admission; preserve observed `ASDUR404`/`ASDUR405` |
| Pump attempt `Refused` | `Busy` |
| Pump attempt `Completed` | `Completed` with the exact result |
| Pump attempt `Unavailable` | `PumpFailed` + `ASDUR103` |
| Pump attempt `Incompatible` after the health precheck race | `PumpFailed` + corresponding `ASDUR108` or `ASDUR400`–`ASDUR403` |
| Caller/budget cancellation | Apply the cancellation-precedence rules above |
| Unexpected non-fatal exception before `pumpStarted` | Log through the host logger; return `ActivationFailed` + `ASDUR407` |
| Unexpected non-fatal exception after `pumpStarted` | Log through the host logger; return `PumpFailed` + `ASDUR407` |

`ASDUR106` and `ASDUR109` are item-level durable outcomes, not activation-service exceptions. They remain in authoritative Work state and contribute to the unchanged aggregate pump `Failed` count. The service must not select one item code to represent a pass.

The bounded catch filter excludes `OperationCanceledException`, `StackOverflowException`, and `OutOfMemoryException`; cancellation follows the phase rules and fatal process failures are not relabeled as an HTTP-capable result.

## Copy-Ready Case Drafts

### Case 0: `epic(durable): ship an executable-contract adoption rail`

#### What problem should this solve?

The native Durable protocol is safe and complete enough for production-shaped use, but a real adopter had to repeat operational-state interpretation, Work identity facts, request construction, pump orchestration, OpenTelemetry activation, and test fakes across seven worker lanes. That repetition produced one correctness-level readiness bug and made the supported scale-to-zero path much harder to discover than the protocol itself.

This case coordinates a dependency-ordered adoption rail without moving application deployment policy into AppSurface.

#### Intended outcome

An adopter can:

- define one immutable typed Work contract;
- register an executor without restating contract facts;
- create requests without manually copying name, version, codec, or provider safety;
- distinguish activation compatibility from runtime readiness;
- test host and lifecycle behavior through a first-party package;
- run either a hosted worker or an authorized external activator through the same bounded pump;
- prove AppSurface activities reach an exporter;
- diagnose schema, epoch, heartbeat, and host-composition problems without exposing secrets;
- start from a generated direct-mapping worker template while the adapter remains experimental.

#### Child cases

- [ ] Case 1: canonical operational assessments.
- [ ] Case 2: typed immutable Work definitions.
- [ ] Case 3: Durable testing package.
- [ ] Case 4: bounded PostgreSQL heartbeat retention.
- [ ] Case 5: configured-target equivalence experiment.
- [ ] Case 6: external-activation service and reference host.
- [ ] Case 7: experimental ASP.NET Core activation adapter.
- [ ] Case 8: non-mutating Durable runtime doctor.
- [ ] Case 9: direct-mapping external-worker project template.

#### Success measures

- Before filing Track B or Track C implementation children, Case 0 records a Skoolit baseline and proposed rewrite in
  `Durable/evidence/executable-contract-adoption.md`, with proposed syntax-highlighted sketches in
  `Durable/evidence/executable-contract-adoption.sketch.cs`, owned by the rail driver. The primary comparison records:
  time to first successful durable completion, the adopter-owned lifecycle decisions that remain explicit, the
  production-relevant failure rows reproducible without ad hoc fakes, and the number of duplicated contract or
  operational facts at each call site. AppSurface-specific line count is a secondary readability guardrail, not the
  product-value verdict.
- A checked-in dependency-free console tool at `tools/ForgeTrust.AppSurface.Durable.AdoptionMetrics` reads
  `Durable/evidence/executable-contract-adoption.measurements.json`, verifies the supplied consumer checkout commit,
  resolves exact unique start/end tokens in either the consumer checkout or repository sketch, and counts every nonblank
  physical source line between them; token lines are excluded and there are no comment or brace exceptions. The three
  named regions are registration, external-activation mapping, and primary lifecycle test. Domain policy, authorization
  implementation, outbox/materialization logic, and application reconciliation must sit outside those regions. Each
  proposed region must contain no more than 25 lines, but a lower count cannot pass the rail if decisions became implicit,
  diagnostics regressed, or the behavioral measures failed.
- The measurement tool writes deterministic JSON with `schemaVersion`, `consumerRepository`, `baselineCommit`, and ordered `regions`; every region contains `variant`, `name`, `sourceRoot` (`consumer` or `repository`), `relativePath`, `startToken`, `endToken`, `lineCount`, `limit`, and `passed`, followed by `overallPassed`. Tests cover missing/duplicate/reversed tokens, commit mismatch, invalid roots, CRLF/LF, blank lines, and deterministic ordering.
- The template smoke test measures elapsed time on `ubuntu-latest`, after package restore and PostgreSQL preparation, from copying valid database configuration plus a per-run random Development activation token into the generated project through an authenticated call that observes one completed Work. The warm-path median of five runs must be under two minutes, and all five timings are published in the same evidence file.
- The public truth table makes it impossible for package guidance or first-party samples to call `NotStarted`, `Stale`, `Draining`, `Incompatible`, or `Unavailable` ready.
- Every first-party external-activation sample uses a host request deadline distinct from the pump discovery budget. Application preparation and cleanup, where present, own separate host-defined tokens outside the adapter.
- One integration proof fails if expected Durable activities are not exported.
- No child introduces Cloud Tasks, OIDC, Skoolit lane types, application outboxes, domain aggregates, or a second durable authority.

#### Non-goals

- No general background-job framework, dashboard, broker, deployment controller, or application workflow engine.
- No replacement for #783 or #765.
- No provider-neutral wake-dispatch interface until another consumer proves the shared transport contract.

### Case 1: `fix(durable): make compatibility and readiness decisions explicit`

#### What problem should this solve?

`DurableRuntimeHealthSnapshot` exposes schema compatibility, epoch compatibility, and the runtime states `Healthy`, `NotStarted`, `Stale`, `Draining`, and `Incompatible`. Consumers still have to reconstruct which combinations authorize deployment, permit a pump attempt, and mean the worker is ready. A real consumer used compatibility as readiness in seven endpoints.

The PostgreSQL implementation also maps a timeout or unavailable database to `Incompatible` with the schema-inconsistent problem code. That collapses “the store disproved compatibility” and “the store could not be observed,” which prevents a doctor or host from giving the correct remediation.

Scale-to-zero also proves that readiness cannot replace compatibility: a deployment may need to validate schema and epoch before external activation is enabled and before a pump heartbeat exists.

#### Proposed shape

Add `DurableRuntimeHealthState.Unavailable`. PostgreSQL connectivity and command-timeout failures map to `Unavailable` with the existing `DurableProblemCodes.StoreUnavailable` (`ASDUR103`) only when the caller's token was not canceled; caller cancellation still propagates. Observed schema or epoch mismatches remain `Incompatible`. Under `Unavailable`, compatibility flags are false because compatibility was not established, not because an incompatibility was observed. Document that distinction on the properties and state.

Add three canonical computed properties to `DurableRuntimeHealthSnapshot`:

```csharp
public bool CanEnableActivation =>
    State != DurableRuntimeHealthState.Unavailable
    && State != DurableRuntimeHealthState.Incompatible
    && SchemaCompatible
    && EpochCompatible;

public bool CanAttemptPump =>
    CanEnableActivation
    && State != DurableRuntimeHealthState.Draining;

public bool IsReady =>
    State == DurableRuntimeHealthState.Healthy
    && CanEnableActivation;
```

`CanEnableActivation` is a deployment/configuration gate. `CanAttemptPump` is only a host admission precheck: the provider remains authoritative and may still reject a concurrent pass or identity takeover. It deliberately permits `NotStarted` so cold activation can establish the first heartbeat and permits `Stale` so the provider can apply its existing guarded takeover rules. `IsReady` is the traffic-readiness decision.

Keep the full state and component facts available for diagnostics. Do not add `IsLive`: process liveness belongs to the host and should not require a durable-store query.

Preserve `IDurableRuntimePump.RunOnceAsync` exactly, including its current process-local overlap exception and empty results from other closed-admission paths. Add a separate opt-in admission-aware contract:

```csharp
public enum DurableRuntimePumpAttemptKind
{
    Completed = 0,
    Refused = 1,
    Unavailable = 2,
    Incompatible = 3,
}

public sealed record DurableRuntimePumpAttempt
{
    public DurableRuntimePumpAttempt(
        DurableRuntimePumpAttemptKind kind,
        DurableRuntimePumpResult? result,
        string? problemCode);

    public DurableRuntimePumpAttemptKind Kind { get; }
    public DurableRuntimePumpResult? Result { get; }
    public string? ProblemCode { get; }
}

public interface IDurableRuntimePumpAdmission
{
    ValueTask<DurableRuntimePumpAttempt> TryRunOnceAsync(
        DurableRuntimePumpRequest request,
        CancellationToken cancellationToken = default);
}
```

`Completed` requires a non-null result and null problem code, including for a legitimate empty pass. `Refused` requires both nullable fields to be null. `Unavailable` requires `ASDUR103`; `Incompatible` requires `ASDUR108` or `ASDUR400`–`ASDUR403`; both require a null result. PostgreSQL implements the new interface and:

- returns `Refused` when the process-local pass slot, runtime admission gate, or `TryBeginPassAsync` guard refuses entry;
- translates a non-caller-canceled Npgsql/timeout failure to `Unavailable`;
- translates `DurableRuntimeSchemaException` to `Incompatible` with the existing schema/epoch code;
- lets caller cancellation and unexpected programming failures propagate.

One internal PostgreSQL failure classifier is shared by health observation and admission-aware pump translation. It owns an
exhaustive table for caller cancellation, provider/command timeout, connection loss, permission/provider failures,
schema/epoch incompatibility, malformed or contradictory provider results, and unexpected programming failures. Only
observed schema/epoch mismatches become `Incompatible`; non-caller-canceled provider failures that prevent an observation
become `Unavailable`; cancellation and unexpected programming/data-contract failures propagate. The implementation shares
the existing pump core but does not change `RunOnceAsync` behavior. External-activation helpers require
`IDurableRuntimePumpAdmission`; existing hosted and direct-pump callers remain source, binary, and behavior compatible.

Add a normative truth table to the Provider and PostgreSQL package documentation:

| State/facts | Enable activation | Attempt pump | Runtime ready | Host liveness consequence |
| --- | ---: | ---: | ---: | --- |
| Compatible + `NotStarted` | Yes | Yes | No | Host decides; a cold activator may establish the heartbeat |
| Compatible + `Healthy` | Yes | Yes | Yes | Host decides |
| Compatible + `Stale` | Yes | Yes | No | Provider applies guarded takeover; alert separately |
| Compatible + `Draining` | Yes | No | No | Expected during shutdown |
| Schema or epoch incompatible | No | No | No | Fail activation closed; host decides whether process remains live |
| Store `Unavailable` | No | No | No | Dependency status is unknown; do not call it incompatible or use it as process liveness |

#### Acceptance criteria

- Public API tests cover every runtime state and all schema/epoch compatibility combinations for all three predicates.
- Attempt-result tests distinguish a completed empty pass, refusal, provider unavailability, and incompatibility without changing existing pump-result semantics.
- `IsReady` is false for contradictory synthetic snapshots even if their state is `Healthy` but a compatibility flag is false.
- PostgreSQL connectivity and command-timeout paths return `Unavailable`/`ASDUR103`; observed missing, old, too-new, inconsistent, and epoch-mismatched states remain `Incompatible` with their existing specific codes.
- The shared PostgreSQL failure-classification matrix covers caller cancellation, deadline cancellation, connection loss,
  permission/provider errors, schema exceptions, malformed query results, and unexpected programming failures through
  both health and admission-aware pump entry points; no downstream host maintains a second exception classifier.
- Provider and PostgreSQL READMEs distinguish compatibility/startup, readiness, and host liveness with copyable examples.
- The PostgreSQL example and tests stop hand-writing readiness expressions.
- A scale-to-zero test proves `NotStarted` may enable and attempt activation before it is ready; a stale-worker test proves the precheck permits the provider's guarded takeover; a drain test proves no new pump attempt is made.
- Existing snapshot constructor behavior remains source compatible; enum and JSON consumers receive release notes for the additive `Unavailable` value.
- Existing `IDurableRuntimePump`, pump-result construction, process-local overlap exception, and closed-admission empty-result behavior remain unchanged. The new interface returns the exact four-kind attempt matrix for process-local overlap, closed runtime admission, drain/takeover races, schema/epoch races, and transient store failures.
- No HTTP endpoint, route name, authorization rule, restart policy, or orchestrator-specific type is added.

#### Dependencies and coordination

This case is the first implementation slice and unblocks cases 3, 6, 7, and 8.

### Case 2: `feat(durable): add immutable typed Work definitions`

#### What problem should this solve?

Registration and request construction currently repeat Work name, version, codecs, and provider-safety facts. `DurableWorkRequest` accepts nine arguments and relies on callers to copy the registration facts correctly. The runtime still fails safely on mismatches, but the supported path makes drift easy to author.

#### Proposed shape

Add an immutable `DurableWorkDefinition<TWork, TResult>` that owns:

- Work name and version;
- typed Work and result codecs;
- provider-safety class;
- default retry policy for future requests;
- the existing `DurableWorkContractIdentity` derived from name and version.

Per-request facts remain outside the definition:

- scope ID;
- command ID;
- idempotency key;
- typed payload;
- optional due time;
- an explicit retry-policy override when the caller intentionally needs one.

The request factory is:

```csharp
DurableWorkRequest CreateRequest(
    DurableScopeId scopeId,
    DurableCommandId commandId,
    string idempotencyKey,
    TWork work,
    DurableWorkRetryPolicy? retryPolicy = null,
    DateTimeOffset? dueAtUtc = null);
```

`null` retry policy selects the definition's immutable default before constructing the existing request, so the accepted fingerprint contains the resolved policy exactly as it does today.

Indicative usage:

```csharp
public static readonly DurableWorkDefinition<SendInvoice, SendInvoiceResult> Definition =
    DurableWork.Define(
        workName: "billing.send-invoice",
        workVersion: "v1",
        workCodec: SendInvoiceCodec.Instance,
        resultCodec: SendInvoiceResultCodec.Instance,
        providerSafety: DurableProviderSafety.ProviderKeyed,
        defaultRetryPolicy: DurableWorkRetryPolicy.Default);

services.AddDurableWork(
    Definition.ExecutedBy<SendInvoiceExecutor>());

var request = Definition.CreateRequest(
    scopeId,
    commandId,
    idempotencyKey,
    new SendInvoice(invoiceId),
    dueAtUtc: null);
```

Freeze the initial binding matrix as:

```csharp
DurableWorkBinding<TWork, TResult, TExecutor>
    DurableWorkDefinition<TWork, TResult>.ExecutedBy<TExecutor>()
    where TExecutor : class, IDurableWorkerExecutor<TWork, TResult>;

DurableReconciledWorkBinding<TWork, TResult, TExecutor, TReconciler>
    DurableWorkBinding<TWork, TResult, TExecutor>.ReconciledBy<TReconciler>()
    where TReconciler : class, IDurableEffectReconciler<TWork, TResult>;

DurableExitWorkBinding<TWork, TResult, TExecutor>
    DurableWorkDefinition<TWork, TResult>.ExecutedByExit<TExecutor>()
    where TExecutor : class, IDurableWorkExitExecutor<TWork, TResult>;

IServiceCollection AddDurableWork<TWork, TResult, TExecutor>(
    DurableWorkBinding<TWork, TResult, TExecutor> binding);

IServiceCollection AddDurableWork<TWork, TResult, TExecutor, TReconciler>(
    DurableReconciledWorkBinding<TWork, TResult, TExecutor, TReconciler> binding);

IServiceCollection AddDurableWork<TWork, TResult, TExecutor>(
    DurableExitWorkBinding<TWork, TResult, TExecutor> binding);
```

The definition, codecs, closed binding value, registration, codec registry, and Work registry are singletons. Executors and reconcilers are transient, matching today's lifetime. `ReconciledBy` rejects a definition whose safety class is not `ReconcileBeforeRetry`. Registering the intermediate `DurableWorkBinding` for a `ReconcileBeforeRetry` definition also fails because the binding is incomplete without a reconciler. Duplicate `(WorkName, WorkVersion)` identities fail deterministically when the service provider constructs the registry, matching today's registry boundary. Registering the same definition twice is still a duplicate and does not silently coalesce.

The legacy registration overloads and #783's `AddDurableWorkExit` overload delegate to the same internal registration builder. `ExecutedByExit<TExecutor>()` requires the definition's provider safety to be `ProviderKeyed`, matching #783 V1; it cannot be reconciler-bound. One `(WorkName, WorkVersion)` may have exactly one binding style in a service provider. Registering legacy, reconciled, and/or exit-aware bindings for the same identity fails with the same duplicate-identity diagnostic rather than selecting by order.

#### Required behavior

- `CreateRequest` encodes the typed payload and copies the definition's exact name, version, provider safety, and default retry policy.
- An explicit retry override is visible at the request call site and remains snapshotted at acceptance.
- Definitions are immutable, thread-safe, validated at construction, and safe to reuse as static values.
- No assembly scan, reflection, global static registry, expression-tree method identity, or implicit runtime-type serializer is introduced.
- Existing `AddDurableWork` and `DurableWorkRequest` APIs remain available during the public-preview migration window.
- The bindings support today's executor/reconciler contracts and #783's final `IDurableWorkExitExecutor<TWork, TResult>` contract without creating a parallel definition type.
- Definitions contain the existing retry-policy default and provider-safety value only. They contain no persisted attempt plan, execution deadline, or typed exit owned by #765 or #783.

#### Acceptance criteria

- Tests prove exact name/version/codec/provider-safety propagation from definition to registration and request.
- Tests prove codec failure, invalid identity, duplicate registration, mismatched binding, explicit retry override, due time, and concurrent request creation.
- Lifetime tests prove singleton definition/codec/registry/registration and transient executor/reconciler behavior.
- Compile-time negative fixtures prove invalid executor/reconciler constraints do not bind, while runtime validation covers safety/reconciler combinations that C# constraints cannot express.
- Compile-only packed-consumer tests prove the intended terse syntax and unchanged legacy syntax.
- A definition cannot silently change the facts of already accepted Work.
- Public/internal API XML documentation explains defaults, immutability, migration, and when direct request construction remains appropriate.
- The Work protocol, package README, diagnostics catalog, and public API baselines are updated.

#### Dependencies and coordination

#783 must land before API review begins. This case consumes its final interface and registration semantics and then unblocks cases 3, 6, 7, and 9; case 8 is independent.

### Case 3: `feat(durable-testing): add deterministic host fakes and lifecycle scenarios`

#### What problem should this solve?

Adopters repeatedly construct large health snapshots, pump results, Work registrations, fake health services, and fake pumps. This makes tests verbose and causes each application to invent its own meaning for compatibility, readiness, cancellation, and pump completion.

#### Proposed shape

Add a test-framework-neutral `ForgeTrust.AppSurface.Durable.Testing` package containing:

- valid-by-default builders for health snapshots, pump requests/results, envelopes, and typed Work requests;
- `FakeDurableRuntimeHealth`, `RecordingDurableRuntimePump`, and `FakeDurableRuntimeDrainControl`;
- definition, registry, codec, and binding observations;
- bounded scenario orchestration over public Durable clients and runtime contracts, split into explicit health-assessment and direct-pump phases;
- completed observation records that permit immediate negative assertions without waiting for a generic timeout.

The package must not implement a fake Work/Flow/Schedule persistence engine and must not reference an assertion library. Builders and fakes return values or immutable observations; the consuming test framework owns assertions. Provider-sensitive recovery, lease, fence, permit, and history scenarios run against the real provider supplied by the test host.

`DurableHostScenario` consumes only the public `IDurableRuntimeHealth` and `IDurableRuntimePumpAdmission` interfaces. It returns two immutable framework-neutral records: `DurableHealthObservation` (snapshot plus elapsed/cancellation facts) and `DurablePumpObservation` (exact attempt, captured request, elapsed/cancellation facts). Provider projects may adapt their real services to those interfaces; the testing package contains no provider adapter. The scenario exposes health predicates but defines no second host-outcome enum; case 6's service owns HTTP-independent activation outcomes.

Indicative usage:

```csharp
var scenario = DurableHostScenario
    .For(SendInvoiceWork.Definition)
    .Using(services)
    .GivenHealth(DurableHealth.NotStartedCompatible());

var health = await scenario.AssessHealthAsync();
Assert.True(health.Snapshot.CanEnableActivation);
Assert.True(health.Snapshot.CanAttemptPump);
Assert.False(health.Snapshot.IsReady);

var pump = await scenario.RunDirectPumpOnceAsync();
Assert.Equal(1, pump.Result.Processed);
```

`AssessHealthAsync` exposes the canonical predicates. `RunDirectPumpOnceAsync` is an explicitly separate provider-contract phase: it bypasses an HTTP endpoint but requires the last observation's `CanAttemptPump`; it does not require readiness because an authorized `NotStarted` host must be able to establish its first heartbeat. No fluent `Expect...` API or alternate activation state machine is included.

#### Acceptance criteria

- Snapshot builders reject internally invalid combinations unless the caller explicitly selects a contradictory-snapshot test mode.
- The recording pump captures exact request bounds, surfaces, cancellation ownership, concurrency, and returned result.
- Scenario APIs have separate overall and inactivity/observation timeouts and use a fake clock where runtime contracts permit it.
- Negative assertions do not wait for the full overall timeout after the scenario's observation window is complete.
- Package tests cover success, cancellation before pump, cancellation after invocation begins, unavailable and incompatible health, stale, drain, busy/concurrent activation, codec failure, and duplicate registration.
- `ForgeTrust.AppSurface.Durable.Testing.Tests` verifies builders, fakes, scenarios, and observations using no provider package.
- `ForgeTrust.AppSurface.Durable.PostgreSql.Tests` references the testing package and uses the same observation records against real PostgreSQL for at least one claim, completion, stale-worker recovery, and post-permit ambiguous outcome. This dependency is one-way; the testing package never references PostgreSQL or Npgsql.
- The package has no dependency on xUnit, NUnit, TUnit, ASP.NET Core, Npgsql, Testcontainers, or a particular assertion library.
- Packed-consumer tests prove observations can be asserted with the repository's supported test frameworks without a transitive assertion dependency.

#### Dependencies and coordination

Depends on cases 1 and 2. PostgreSQL conformance remains in the provider test project and consumes reusable public-contract scenarios rather than introducing a fake provider or a provider dependency into the testing package.

### Case 4: `fix(durable-postgresql): bound runtime-heartbeat retention`

#### What problem should this solve?

`runtime_heartbeat` keeps one row per configured worker identity. Process-plus-GUID identities are valid, and abrupt process loss cannot clean up its own row. No migration, provider path, or operator API currently prunes stale identities, so normal deployment churn can grow the relation without bound.

#### Proposed shape

Add provider-owned, opportunistic heartbeat maintenance:

- default retention horizon: 30 days;
- default maintenance cadence: 24 hours per process;
- default delete batch: 500 rows;
- retention must be from 24 hours through 3,650 days and longer than the configured stale threshold (currently bounded to one hour); provider validation must reject an unsafe relationship;
- never delete the current worker instance, a row with a heartbeat at or after the cutoff, or a row updated by heartbeat renewal/takeover after selection;
- acquire a non-blocking provider advisory lock so only one process performs a batch at a time;
- expose low-cardinality attempted/deleted/failure telemetry without worker identifiers.

Use exact migration `0010_runtime_heartbeat_retention.sql`. It is applied while holding the existing package migration lock and contains:

```sql
SET LOCAL lock_timeout = '5s';

CREATE INDEX ix_runtime_heartbeat_retention
    ON appsurface_durable.runtime_heartbeat (last_heartbeat_at, worker_id);

CREATE FUNCTION appsurface_durable.prune_runtime_heartbeats(
    p_retention interval,
    p_maximum_rows integer,
    p_current_worker_id text,
    p_current_worker_instance_id uuid)
RETURNS integer
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = pg_catalog, appsurface_durable
AS $$
DECLARE
    v_cutoff timestamp with time zone;
    v_deleted integer;
BEGIN
    IF p_retention IS NULL
       OR p_retention < interval '24 hours'
       OR p_retention > interval '3650 days' THEN
        RAISE EXCEPTION 'p_retention must be between 24 hours and 3650 days.'
            USING ERRCODE = '22023';
    END IF;
    IF p_maximum_rows IS NULL OR p_maximum_rows NOT BETWEEN 1 AND 5000 THEN
        RAISE EXCEPTION 'p_maximum_rows must be between 1 and 5000.'
            USING ERRCODE = '22023';
    END IF;
    IF p_current_worker_id IS NULL
       OR length(p_current_worker_id) NOT BETWEEN 1 AND 200
       OR p_current_worker_id !~ '^[A-Za-z0-9._:-]+$' THEN
        RAISE EXCEPTION 'p_current_worker_id is invalid.'
            USING ERRCODE = '22023';
    END IF;
    IF p_current_worker_instance_id IS NULL
       OR p_current_worker_instance_id =
          '00000000-0000-0000-0000-000000000000'::uuid THEN
        RAISE EXCEPTION 'p_current_worker_instance_id is invalid.'
            USING ERRCODE = '22023';
    END IF;

    -- Two-int advisory namespace "ASDU"/"RHBR"; it is distinct from the
    -- one-bigint schema migration lock and from hashed per-scope locks.
    IF NOT pg_try_advisory_xact_lock(1095976021, 1380467282) THEN
        RETURN 0;
    END IF;

    v_cutoff := clock_timestamp() - p_retention;
    WITH candidates AS
    (
        SELECT heartbeat.worker_id, heartbeat.worker_instance_id
        FROM appsurface_durable.runtime_heartbeat AS heartbeat
        WHERE heartbeat.last_heartbeat_at < v_cutoff
          AND NOT
          (
              heartbeat.worker_id = p_current_worker_id
              AND heartbeat.worker_instance_id = p_current_worker_instance_id
          )
        ORDER BY heartbeat.last_heartbeat_at, heartbeat.worker_id
        FOR UPDATE SKIP LOCKED
        LIMIT p_maximum_rows
    )
    DELETE FROM appsurface_durable.runtime_heartbeat AS heartbeat
    USING candidates
    WHERE heartbeat.worker_id = candidates.worker_id
      AND heartbeat.worker_instance_id = candidates.worker_instance_id
      AND heartbeat.last_heartbeat_at < v_cutoff;

    GET DIAGNOSTICS v_deleted = ROW_COUNT;
    RETURN v_deleted;
END;
$$;

REVOKE ALL ON FUNCTION appsurface_durable.prune_runtime_heartbeats(
    interval, integer, text, uuid) FROM PUBLIC;
```

The migration must run as the documented `migration_owner_role`; the schema tests assert that this role owns the function after migration and role-recipe application. The role recipe must:

- add `'appsurface_durable.prune_runtime_heartbeats(interval, integer, text, uuid)'::regprocedure` to the runtime role's exact function allowlist;
- execute `REVOKE ALL ON FUNCTION ... FROM PUBLIC`, dispatcher, and retention-operator roles;
- execute `GRANT EXECUTE ON FUNCTION ... TO :runtime_role`;
- continue rejecting effective function privileges outside the complete allowlist;
- leave table `DELETE`, ownership, grant options, and role membership prohibited.

The role-recipe delta is explicit:

```sql
-- In the runtime allowlist predicate:
AND routine.oid IN
(
  'appsurface_durable.runtime_due_dispatch_health(integer)'::pg_catalog.regprocedure,
  'appsurface_durable.prune_runtime_heartbeats(interval, integer, text, uuid)'::pg_catalog.regprocedure
)
AND privilege.privilege_name = 'EXECUTE'

SELECT format(
  'ALTER FUNCTION appsurface_durable.prune_runtime_heartbeats(interval, integer, text, uuid) OWNER TO %I',
  :'migration_owner_role') \gexec
REVOKE ALL ON FUNCTION appsurface_durable.prune_runtime_heartbeats(
  interval, integer, text, uuid) FROM PUBLIC;
SELECT format(
  'REVOKE ALL ON FUNCTION appsurface_durable.prune_runtime_heartbeats(interval, integer, text, uuid) FROM %I',
  :'dispatcher_role') \gexec
SELECT format(
  'REVOKE ALL ON FUNCTION appsurface_durable.prune_runtime_heartbeats(interval, integer, text, uuid) FROM %I',
  :'retention_operator_role') \gexec
SELECT format(
  'GRANT EXECUTE ON FUNCTION appsurface_durable.prune_runtime_heartbeats(interval, integer, text, uuid) TO %I',
  :'runtime_role') \gexec
```

The function intentionally ignores `pass_active` because an abruptly lost process may leave that flag true. `FOR UPDATE SKIP LOCKED`, the exact identity join, and the single cutoff prevent deletion of a locked, renewed, or replaced row. The two-int advisory namespace avoids collision with the existing one-bigint migration lock; lock refusal is a successful no-op.

The runtime calls the function at most once per configured cadence after it has established its current heartbeat. The command uses the provider's existing bounded command timeout and cancellation; the advisory lock never waits, candidate row locks are skipped, and a timeout rolls back the entire batch. The provider remains safe if maintenance never runs; cleanup is capacity management, not correctness.

Deployment is forward-only: run `durable schema preflight`, drain and stop old runtimes, apply migration 0010 as migration owner, reapply the exact role recipe, rerun preflight as the runtime role, deploy the schema-10 binary, then resume activation. `SET LOCAL lock_timeout` makes index lock contention fail and roll back rather than wait indefinitely. Preflight must flag migration 0010 as a downtime migration while it is pending and verify the exact function owner, `prosecdef`, `proconfig` search path, `PUBLIC` revoke, runtime execute grant, and retention index after it lands. Before the migration starts, rollback may redeploy the old binary. After migration 0010 commits, do not down-migrate or assume a schema-9 binary accepts schema 10; hold activation closed and roll forward unless mixed-version compatibility metadata explicitly proves the old binary supports version 10.

#### Acceptance criteria

- Fake-clock provider tests cover no-op before cadence, one call after cadence, multiple calls over time, and transient database failure; SQL cutoff uses authoritative database time.
- Real PostgreSQL tests prove recent rows, the exact current worker instance, a concurrently renewed row, and rows at the exact cutoff survive while eligible older rows are deleted.
- Abrupt-process-loss and process-plus-GUID tests prove old rows eventually leave without shutdown cooperation.
- Concurrent-call tests prove the advisory lock and `SKIP LOCKED` path never exceed the requested batch and do not block heartbeat renewal or takeover.
- Role tests prove runtime cannot directly delete or truncate heartbeat rows, `PUBLIC` has no function access, and runtime can execute only the exact constrained function signature.
- Migration checksum, required schema version 10, function allowlist, role recipe, schema status, script generation, preflight, forward deployment, and no-down-migration guidance remain coherent.
- Cleanup failure does not fail a pump pass, change readiness by itself, or hide a real runtime failure.
- Scale proof seeds 100,000 eligible stale rows plus 1,000 recent rows, records `EXPLAIN (ANALYZE, BUFFERS)` for one 500-row batch, proves the retention index supplies candidate order, deletes no more than 500 rows, observes no lock wait, and completes within the provider's existing command timeout.

#### Dependencies and coordination

Independent of cases 1 through 3. Case 8 may report retention configuration and last maintenance outcome after this lands.

### Case 5: `design(durable-postgresql): prove configured-target equivalence without exposing credentials`

#### What problem should this solve?

Hosts using separate dispatcher, runtime, migration-owner, and application data sources sometimes need to verify that two configurations name the same intended PostgreSQL endpoint without logging or comparing raw connection strings. A downstream consumer built its own parser and equivalence rules, but a general public API is security-sensitive and not required for the core rail.

#### Proposed shape

Run this as an evidence-gated API-design case. The initial supported normalization is deliberately narrow:

- parse with the repository's pinned `NpgsqlConnectionStringBuilder`;
- require an explicit nonempty database so a username-derived database cannot leak into identity semantics;
- accept either one network host or one Unix-domain socket path; reject multi-host/failover forms in v1 rather than guessing their equivalence;
- canonicalize IPv4 and IPv6 with `IPAddress`, canonicalize a DNS host with ASCII IDNA plus invariant lowercase, and compare a Unix socket path with ordinal case-sensitive semantics;
- use the parsed port, including Npgsql's default when omitted;
- include the normalized `Target Session Attributes` value because it changes which endpoint is acceptable;
- compare the database with ordinal semantics.

The candidate immutable tuple is `(FormatVersion=1, Transport, HostOrSocket, Port, Database, TargetSessionAttributes)`. It excludes username, password, client certificate secrets, tokens, application name, pooling, timeout, and every other credential/session option. The fingerprint is lowercase SHA-256 over UTF-8 fields prefixed with `appsurface-postgresql-target-v1`, encoded with length prefixes rather than delimiters. The display form is an explicit diagnostic method, not `ToString`; `ToString` returns only the type name.

Before any public API is approved, implement the algorithm as an internal prototype and run the same corpus against the Skoolit parser plus one distinct production-shaped consumer. If the second consumer needs multi-host aliases, DNS resolution, or different database-default semantics, stop and redesign instead of broadening the comparer opportunistically.

Name the operation configured-target equivalence. It must not claim that DNS aliases, proxies, failover endpoints, or two connection strings identify the same physical cluster. Connected StoreId and epoch evidence remain authoritative.

#### Acceptance criteria

- Tests cover keyword order, DNS casing and IDNA, default and explicit ports, IPv4, IPv6, Unix sockets, whitespace, explicit database semantics, malformed strings, aliases, credential differences, and deterministic rejection of multi-host and implicit-database configurations.
- Passwords, usernames, certificates, tokens, arbitrary options, and original connection strings never appear in `ToString`, equality diagnostics, exceptions, fingerprints, logs, or snapshots.
- Equivalent configured targets compare equal across dispatcher/runtime credentials; materially different host, port, or database targets compare unequal.
- DNS resolution and live connections are never performed by parsing or equality.
- Public docs distinguish configuration equivalence from physical-store identity and recommend authoritative StoreId/epoch checks after connecting.
- The case records both consumer corpora and an API-review decision. With only one suitable consumer, keep the prototype internal or close the feature as not yet justified.

#### Dependencies and coordination

Not a blocker for Tracks A or B or for the runtime doctor. Start only after a second consumer is named; it may otherwise run in parallel with cases 6 and 7.

### Case 6: `feat(durable): add an external-activation service and reference host`

#### What problem should this solve?

The current PostgreSQL example proves passive registration, explicit hosting, schema/epoch checks, one pump pass, and health. It does not give an adopter one complete scale-to-zero host showing separate deployment/admission/readiness semantics, authorization, budget ownership, tests, and exported activities.

#### Proposed shape

Add a focused ASP.NET Core reference host and integration-test project. Before case 7, use ordinary ASP.NET Core endpoint mapping so the example establishes the minimum correct baseline the adapter must reduce.

The reference host must show:

- passive PostgreSQL registration without `AddWorkerHost()`;
- one typed Work definition from case 2;
- the canonical public-preview `IDurableExternalActivationService`, request, result, outcome enum, cancellation precedence, and failure mapping defined in the normative section;
- an application-owned process liveness endpoint with no Durable store dependency;
- a compatibility/startup endpoint using `CanEnableActivation`;
- a readiness endpoint using `IsReady`;
- a private activation endpoint protected by an explicit authorization policy;
- a request/infrastructure deadline distinct from `DurableRuntimePumpRequest.TimeBudget`;
- one call to the authoritative admission-aware `IDurableRuntimePumpAdmission`;
- exact use of the normative activation flow for unavailable, incompatible, not-started, healthy, stale, draining, provider-refused/busy, pre-admission cancellation, request-budget exhaustion, completed, pump-canceled, and pump-failed outcomes;
- AppSurface OpenTelemetry registration plus a low-cardinality
  `appsurface.durable.runtime.activation` activity emitted by the transport-neutral service and delivered to an in-memory
  exporter. The activity contains only contract version, phase/outcome, observed health state, and bounded problem code;
  it contains no payload, scope, Work identity, worker identity, route, authorization principal, or exception text.

Case 6 is the sole owner of that transport-neutral mapping. The direct endpoint translates its result to host-selected HTTP status/body; case 7 must consume the same types and service unchanged. The sample uses a generic payload-free wake request. It contains no Cloud Tasks, OIDC provider setup, application activation outbox, or domain continuation logic. A separate application-owned example wrapper may show materialization and cleanup before/after the handler with its own bounded tokens, but those hooks are not part of Durable activation and their failures are not encoded as pump outcomes.

#### Acceptance criteria

- A cold-start test passes compatibility while the runtime is `NotStarted`, enables activation, runs one pass, and then reaches `Healthy`.
- Readiness returns failure for `NotStarted`, `Stale`, `Draining`, `Incompatible`, and `Unavailable`.
- Liveness remains successful during a temporary PostgreSQL outage while compatibility/readiness report `Unavailable`.
- An outer request timeout does not reuse the pump discovery budget. Cancellation after pump invocation produces only `PumpCanceled`; documentation directs callers to durable state and #783 exits rather than inferring whether an effect occurred.
- Concurrent activation requests have deterministic bounded behavior and cannot start overlapping passes beyond the provider contract.
- Table-driven service tests cover every normative outcome, cancellation-precedence branch, and failure-mapping row before the ASP.NET adapter exists.
- The integration test configures `Sdk.CreateTracerProviderBuilder()`, adds source
  `AppSurfaceActivitySources.ActivitySourceName` (`ForgeTrust.AppSurface`), uses `AlwaysOnSampler` plus the in-memory
  exporter, invokes the real activation service through the reference endpoint, calls `ForceFlush`, and asserts exported
  operation `appsurface.durable.runtime.activation`, `ActivityKind.Internal`, contract version `1`, final phase/outcome,
  and absence of payload, scope, Work, worker, principal, route, or exception tags. It distinguishes “an activity started,”
  “a listener observed it,” and “an exporter received it.”
- Documentation states explicitly that this proves external-activation service and standalone-host
  listener/provider/exporter composition, not native Work claim/execution spans. Native Work pump/claim spans remain a
  separate observability case rather than an implied result of this rail.
- Documentation explains the difference between emitting an `ActivitySource` and configuring an OpenTelemetry listener/provider/exporter.
- Documentation compares native Durable, the Workers projection contract, and the Durable Task Flow adapter.
- The example, tests, package links, and release proof run from a packed consumer.

#### Dependencies and coordination

Depends on cases 1, 2, and 3. Its measured direct-mapping orchestration and outcome matrix become the baseline for case 7.

### Case 7: `feat(durable-aspnetcore): prototype authorized external activation`

#### What problem should this solve?

Seven downstream routes repeat the same outer sequence around a bounded pump, but they also contain application-owned materialization, continuation, reconciliation, and deployment policy. AppSurface needs evidence that a thin ASP.NET Core adapter can remove the mechanical repetition without taking ownership of those policies.

#### Proposed shape

Add an explicitly experimental `ForgeTrust.AppSurface.Durable.AspNetCore` package. The mapping API must require:

- an explicit route pattern;
- an explicit host authorization policy;
- a bounded transport-neutral activation request or factory;
- an application response mapper over a transport-neutral activation outcome.

Consume case 6's public-preview `IDurableExternalActivationService`, request, result, outcome enum, cancellation precedence, and failure mapping unchanged. This case owns no second health-to-outcome or exception-to-code mapping. Only the ASP.NET package and mapping carry the .NET `[Experimental]` diagnostic until the graduation gate passes.

The ASP.NET mapping is a thin transport layer:

```csharp
RouteHandlerBuilder MapDurableExternalActivation(
    this IEndpointRouteBuilder endpoints,
    string pattern,
    string authorizationPolicy,
    Func<HttpContext, DurableExternalActivationRequest> requestFactory,
    Func<HttpContext, DurableExternalActivationResult, IResult> responseMapper);
```

It resolves the pre-composed service, passes `HttpContext.RequestAborted`, invokes the response mapper, and applies `RequireAuthorization(authorizationPolicy)`. It has no before/after/recovery participant model. Application materialization, continuation, reconciliation, cleanup, outbox acknowledgement, and domain transitions compose outside this call with host-owned tokens and errors.

The mapping returns `RouteHandlerBuilder` so the host can add metadata and stricter authorization. It must never default to anonymous access.

The canonical result remains payload-free and carries no scope/aggregate identity, provider response, arbitrary exception, HTTP status, or recovery verdict. The host's response mapper decides status codes and body shape. Unexpected request-factory or response-mapper exceptions remain ordinary host exceptions; the adapter does not relabel application failure as Durable state.

#### Graduation gates

The package remains experimental until:

1. the reference host and one distinct host/adopter use the same public contract;
2. the representative downstream lane is first mapped directly over case 6's service, then through the adapter; the measured adapter region shrinks by at least 30% from that same-lane direct baseline and remains at or below 25 nonblank lines, without importing application types;
3. every row in the normative activation flow, including unavailable health, stale takeover, busy admission, exhausted request budget, pump `HasMore`, outer cancellation, and cancellation after pump invocation, has deterministic tests;
4. no required composition point needs a Cloud Tasks, OIDC, lane, outbox, cleanup, or domain-state concept;
5. security review confirms explicit authorization and bounded diagnostic output;
6. API review confirms that the transport-neutral result is useful outside HTTP.

If those gates fail, keep the proof code example-internal or remove the package before stable publication.

#### Acceptance criteria

- Mapping without a named authorization policy fails during startup with an actionable diagnostic.
- `RequestBudget` and pump `TimeBudget` are validated for positive bounds and documented ownership; pump `TimeBudget` is never converted into the outer hard deadline.
- Case 6's service contract tests remain the sole proof of phase order, cancellation precedence, and exception mapping; this package neither copies nor overrides them.
- Endpoint tests feed each canonical result from a fake service and prove the exact same value reaches the host response mapper without reclassification.
- Cancellation tests prove `HttpContext.RequestAborted` is forwarded as the service caller token; request-budget ownership remains in the canonical service.
- Response mapping cannot access payloads or arbitrary provider exceptions through the activation outcome.
- Endpoint tests cover authentication/authorization, every outcome kind, request-factory and response-mapper exceptions, cancellation races, concurrent requests, and application response mapping.
- The reference host is migrated to the adapter and the Case 0 measurement tool records at least a 30% reduction in the external-activation mapping region, with at most 25 nonblank lines and no hidden health/budget facts.
- A separate host wrapper test demonstrates that application preparation and cleanup remain outside the service and can continue under a host-lifetime token after request cancellation.

#### Dependencies and coordination

Depends on cases 1, 2, 3, and 6. Stable publication requires the graduation decision; case 9 does not wait and uses direct mapping.

### Case 8: `feat(cli): add non-mutating Durable runtime doctor diagnostics`

#### What problem should this solve?

Durable startup, health, CLI, logs, and tests expose related facts but do not give an adopter one non-mutating diagnosis containing the problem, likely cause, safe fix, and canonical documentation link. The first doctor should diagnose the connected store and runtime configuration only. Definition discovery, host telemetry configuration, and physical target equivalence require evidence the CLI does not possess.

#### Proposed shape

Extend the existing `appsurface durable` command family with:

```console
appsurface durable doctor \
  --connection-env APPSURFACE_DURABLE_CONNECTION \
  --runtime-epoch-env APPSURFACE_DURABLE_RUNTIME_EPOCH \
  [--worker-id <privacy-safe-id> --stale-after <duration>] \
  [--timeout <duration>] \
  [--format text|json]
```

`--connection-env` reuses the existing schema-command behavior and defaults to `APPSURFACE_DURABLE_CONNECTION`; no connection-string argument is accepted. For doctor, it must resolve to the restricted runtime-role credential, not migration owner. `--runtime-epoch-env` defaults to `APPSURFACE_DURABLE_RUNTIME_EPOCH` and must resolve to a nonempty GUID. `--worker-id` and `--stale-after` must appear together; the latter is the host's actual configured stale threshold, not a CLI guess. `--timeout` defaults to ten seconds and accepts one second through two minutes. `--format` defaults to `text`.

The command may read:

- schema compatibility and migration status;
- configured and active runtime epoch;
- provider StoreId or equivalent authoritative connected-store identity;
- the selected worker's heartbeat age and state, when a worker ID is supplied;
- whether migration 0010's heartbeat-retention capability is installed.

Retention capability is not inferred from migration number alone. One bounded catalog query resolves exact regprocedure `appsurface_durable.prune_runtime_heartbeats(interval,integer,text,uuid)` and verifies:

- the function owner equals the `appsurface_durable` schema owner;
- `prosecdef` is true;
- `proconfig` contains exactly the required fixed search path;
- the current runtime credential has `EXECUTE`;
- the function ACL has no `PUBLIC` grantee and grants no grant option to the current credential;
- `ix_runtime_heartbeat_retention` exists on the exact table and `pg_get_indexdef` matches `(last_heartbeat_at, worker_id)`.

The command reports capability unavailable if any invariant fails; it never calls the pruning function. Existing role checks reject a current credential that owns Durable objects, has `BYPASSRLS`, or holds grant options, with guidance to supply the runtime role.

Heartbeat observation uses one SQL statement that captures `clock_timestamp()` once as `observed_at_utc` and computes nonnegative age from that same value. Rendering receives an immutable observation and never calls the process clock. Unit snapshots use a fixed observation; PostgreSQL integration tests assert the database timestamp within a bounded wall-clock range rather than snapshotting a live timestamp.

The command does not accept or compare a second connection string, infer host OpenTelemetry listener/exporter state, or inspect Work definitions. A later manifest case may be designed only if case 2 can produce a deterministic compile-time artifact from the same source facts without loading or executing a consumer assembly. That future case is not part of this rail's success.

Use one stable diagnostic descriptor model across health guidance, CLI output, logs, testing assertions, and `troubleshooting/durable-diagnostics.md`:

```text
code
problem
likely cause
safe fix
canonical docs link
```

#### Acceptance criteria

- The default command is non-mutating and never applies migrations, rotates epochs, drains workers, retries Work, or changes retention.
- Human and JSON output are deterministic, bounded, secret-free, and covered by snapshots.
- With worker inputs, diagnostics distinguish not-started/worker-not-found, current, stale, draining, schema incompatible, epoch incompatible, retention capability absent, and unavailable database. Without worker inputs, the command makes no heartbeat/readiness claim.
- Exit code 0 means no finding in the requested checks, 2 means an actionable compatibility or worker finding, 3 means invalid input, and 4 means the dependency was unavailable. Unexpected CLI failure retains the CLI's existing general failure code.
- Raw connection strings, credentials, payloads, scope/aggregate IDs, arbitrary exceptions, and trace context never appear.
- Tests prove the exact connection/epoch/optional-worker inputs from command line and environment, precedence rules, bounded timeouts, cancellation, unavailable dependencies, and no interactive prompts.
- Catalog mutation tests independently break function signature, owner, security-definer flag, search path, runtime execute privilege, `PUBLIC` ACL, grant option, index presence, and index shape; each produces one bounded diagnostic without invoking maintenance.
- Text and JSON snapshot tests inject one fixed database observation so heartbeat age and timestamps are byte-deterministic.
- Documentation states what the doctor cannot prove, including registered Work definitions, physical target equivalence, provider-effect truth, application authorization, and Activity listener/exporter delivery.

#### Dependencies and coordination

Depends on cases 1 and 4. It does not depend on definitions, target comparison, the reference host, or the experimental adapter.

### Case 9: `feat(durable): add a direct-mapping external-worker project template`

#### What problem should this solve?

Even after the APIs exist, a first-time adopter must assemble package references, PostgreSQL roles, schema preflight, Work definitions, registration, host health, authorization, OpenTelemetry, and tests. Missing one step can leave a worker safe but inert or observable only through database inspection.

#### Proposed shape

Publish a `dotnet new` template with an explicit, discoverable name such as:

```console
dotnet new appsurface-durable-worker
```

No template package or `.template.config/template.json` exists in the repository today. This case therefore owns a small
packable template project, its template manifest and classifications, package-index entry, NuGet metadata, generated-file
allowlist, and CI proof that installs the freshly packed artifact into an isolated `DOTNET_CLI_HOME`. It reuses the existing
NuGet feed, package signing/provenance, preview/stable promotion, and release workflows; it does not assume nonexistent
template-specific packaging.

The generated project contains:

- one typed sample Work definition and executor;
- passive PostgreSQL provider registration;
- separate dispatcher and runtime credentials with secret placeholders; the dispatcher credential is wired only to payload-free global discovery and the runtime credential to scoped mutation/heartbeat, matching the provider constructor and role recipe;
- explicit schema preflight guidance and no automatic DDL;
- the stable direct ASP.NET Core endpoint mapping proven by case 6, calling `IDurableExternalActivationService` without the experimental adapter package;
- a required `ActivationAuthorization` policy that fails startup closed until a host scheme is configured;
- an opt-in development/CI-only bearer-token scheme enabled only in `Development` when `APPSURFACE_TEMPLATE_ACTIVATION_TOKEN` is present; it compares the token in fixed time, has no default value, and is rejected in every other environment;
- a generated caller script/test client that sends the configured bearer token;
- liveness, compatibility, and readiness endpoints with distinct meanings;
- OpenTelemetry listener/provider/exporter activation;
- one first-party lifecycle scenario test;
- local PostgreSQL instructions and a no-secret checked-in configuration;
- links to the Work protocol, PostgreSQL deployment guide, diagnostics catalog, native Durable/Workers/Durable Task comparison, and release guidance.

#### Acceptance criteria

- Template installation, creation, restore, build, test, format, and local run pass in CI from a clean temporary directory.
- No generated secret, fixed production credential, anonymous activation route, automatic migration, or broad database privilege exists.
- Production startup without an application-selected authentication scheme fails with an actionable message. The development scheme fails when its token is absent, cannot activate outside `Development`, rejects missing/wrong tokens, and CI generates a random token per run.
- Registration tests prove the dispatcher data source is used only for payload-free discovery and the runtime data source for scoped operations; documentation links both credentials to their exact role-recipe grants.
- The generated test proves compatible-but-not-started, one successful activation, readiness after a heartbeat, and an exported activity.
- A generated project can replace the sample Work with a new definition without editing framework plumbing in more than the documented locations.
- The template uses only stable APIs and contains no experimental adapter reference.
- Package/repository docs make the template the fastest start while preserving the lower-level manual path for advanced hosts.
- The new template pack project is present in the solution and package index; package, prerelease, stable-release, and
  packed-consumer gates install the exact produced `.nupkg`, list `appsurface-durable-worker`, create a project, and run its
  verification path in a clean temporary directory. Distribution reuses the existing NuGet publication path; no separate
  installer, feed, or service is introduced.
- If case 7 later graduates, a separate measured template-update case may replace the direct mapping; graduation does not silently change an existing template.

#### Dependencies and coordination

Depends on cases 1, 2, 3, and 6. Cases 7 and 8 are not required for first publication.

## Open Questions and Evidence Gates

1. **Adapter graduation:** what distinct second host will exercise the experimental mapping? The reference host plus seven lanes in one application provide breadth, but not independent adoption.
2. **Target comparison:** can the deliberately narrow single-target algorithm satisfy both named consumers? Multi-host support requires a later versioned design rather than expansion during implementation.
3. **Heartbeat cleanup cadence:** the proposed 30-day/24-hour/500 defaults require scale proof against expected deployment churn before they become public defaults.
4. **Future manifest:** can a compile-time manifest be derived from the typed definition without restricting ordinary C# composition into an awkward DSL or creating a second identity source? This is explicitly outside the current doctor and rail success.

These are implementation evidence gates, not reasons to collapse the case boundaries.

## Success Criteria

- The operational-state truth table is public, tested, and used by every first-party Durable sample.
- One typed definition feeds registration and request creation without repeated Work name, version, codec, provider safety, or default retry facts.
- The testing package replaces hand-built health and pump fakes in the reference host and one downstream lane.
- PostgreSQL heartbeat storage remains bounded under process-plus-GUID identity churn without granting runtime direct deletion.
- The target-comparison experiment either proves a deliberately narrow credential-safe public contract against two consumers or records why it remains internal/deferred.
- The reference host proves cold activation, correct readiness, separate time budgets, authorization, and exported activities.
- The experimental ASP.NET adapter either passes every graduation gate or is explicitly retained as example-only/experimental; it does not drift into a silent stable dependency.
- `appsurface durable doctor` produces code/problem/cause/fix/docs guidance without mutation or secret leakage.
- The generated direct-mapping template reaches one successful Work execution in the measured warm-path budget and excludes the experimental adapter.
- Package-level and repository-level docs explain native Durable, Workers projection, and Durable Task boundaries.
- A downstream audit finds no new application-specific type or policy in Durable core.

## Distribution Plan

- Cases 1 and 2 ship additively in the coordinated `ForgeTrust.AppSurface.Durable` and `ForgeTrust.AppSurface.Durable.Provider` public-preview package family.
- Case 3 ships as a separate `ForgeTrust.AppSurface.Durable.Testing` package so production applications do not take test dependencies.
- Case 4 ships in `ForgeTrust.AppSurface.Durable.PostgreSql` with forward-only migration, role-recipe, and packed-provider evidence.
- Case 5 produces an internal prototype and API decision first; only a successful two-consumer proof adds a public PostgreSQL package type.
- Case 6 ships the transport-neutral service/contracts additively in `ForgeTrust.AppSurface.Durable.Provider`, plus a checked-in runnable example and integration-test project.
- Case 7 begins as an explicitly experimental `ForgeTrust.AppSurface.Durable.AspNetCore` package or namespace and follows the repository's preview publication rules only after security and package review.
- Case 8 extends the existing AppSurface CLI artifact and release path.
- Case 9 adds an explicit .NET template pack project and ships it through the existing NuGet/package workflow; the
  repository currently has no established template package to extend.
- Existing GitHub Actions package, release, public API, documentation, packed-consumer, and PostgreSQL verification remain the CI/CD path. No web service, container image, broker, or new package feed is required.

## Dependencies

- [#783](https://github.com/forge-trust/AppSurface/issues/783) owns typed Durable Work exits and is a hard dependency of case 2; its executor and registration contracts must land before the definition binding API is reviewed.
- [#765](https://github.com/forge-trust/AppSurface/issues/765) owns deterministic persisted attempt plans and execution deadlines. Definitions may later supply defaults, but this rail does not duplicate its storage semantics.
- [#747](https://github.com/forge-trust/AppSurface/issues/747) already exposes registered Work contract identities and preserves scoped discovery before claim.
- The closed [portable Durable runtime epic #641](https://github.com/forge-trust/AppSurface/issues/641) remains the protocol baseline.
- The current `examples/durable-postgresql` application, Provider contracts, PostgreSQL heartbeat migration, observability package, CLI schema commands, diagnostics catalog, public API snapshots, and package verification are the main reuse points.

## Next Steps

1. Perform the assignment below against one representative Skoolit lane and record the before/after AppSurface-specific line counts.
2. Create case 0 with links to #783, #765, #747, and this design.
3. Create cases 1 and 4 first. Mark 1 as the P1 correctness slice, and finish #783 in parallel.
4. Create case 2 only after #783 lands; create case 3 after cases 1 and 2 are accepted.
5. Build case 6 as the canonical transport-neutral service and direct-mapping baseline, then constrain case 7 to transport-only adaptation.
6. Open case 8 as store/runtime diagnostics only; keep manifest, target comparison, and telemetry inference out.
7. Create case 9 after the stable case 1–3 and case 6 APIs are known; use direct mapping over the case 6 service.
8. Open case 5 only after naming a second configured-target consumer. Case 7 may proceed experimentally but may not graduate without a second adapter adopter.

## The Assignment

Before filing the child cases, sketch one real Skoolit lane using exactly three proposed touchpoints:

1. one `DurableWorkDefinition<TWork, TResult>`;
2. one direct host mapping using `CanEnableActivation`, `CanAttemptPump`, `IsReady`, a host request deadline, and a distinct pump discovery budget;
3. one `DurableHostScenario`.

Record the consumer repository, baseline commit, exact files, unique start/end tokens, exclusions kept outside the regions, raw baseline counts, and proposed counts in the measurement JSON and narrative evidence file. Run:

```console
dotnet run --project tools/ForgeTrust.AppSurface.Durable.AdoptionMetrics -- \
  --spec Durable/evidence/executable-contract-adoption.measurements.json \
  --consumer-root /path/to/skoolit \
  --output Durable/evidence/executable-contract-adoption.results.json
```

The tool counts every nonblank physical line between the tokens and must report `overallPassed: true`. Reject or revise any proposed upstream hook that needs a Cloud Tasks type, OIDC claim, lane-selection rule, application outbox row, or domain state. The sketch succeeds when application policy remains obvious and each proposed reusable region contains no more than 25 AppSurface-specific lines.

## What I Noticed About How You Think

- You said, "Take this review and lets draft cases to solve these concerns." You moved directly from critique to an issue structure rather than asking for a broad rewrite.
- You approved the boundary with, "Agreed this is a good boundary for now." The words "for now" preserve the evidence gate instead of pretending today's application shape is permanent platform truth.
- You chose the complete staged rail over the smaller patch train after seeing the cost and dependency graph. That keeps the correctness fix first without treating it as the ceiling.
- You accepted the experimental-adapter challenge while keeping stable publication gated. That is the right kind of reversibility for a public-preview API.

## Autoplan Review

### Phase 1 — CEO premise and scope review

#### Premise challenge

| Premise | Assessment | Decision |
| --- | --- | --- |
| Seven Skoolit lanes reveal reusable Durable friction | Proven for one adopter, not yet for the market | Keep Skoolit as the first evidence source; require a distinct adopter before stable promotion of convenience surfaces |
| Operational interpretation belongs upstream | Proven by the repeated readiness error and existing provider-owned facts | Accept; case 1 is the first correctness slice |
| A typed definition can remove drift without becoming authority | Sound if PostgreSQL keeps snapshotted request facts | Accept after #783 freezes the executor/exit boundary |
| Fewer framework-specific lines prove better adoption | Useful readability signal but gameable as a value metric | Demote line count to a guardrail; make time, explicit decisions, failure reproduction, and duplicated facts primary |
| External activation is reusable without owning transport policy | Sound when the service ends at health/admission/pump outcomes | Accept; keep routes, auth, wake transport, domain preparation, and cleanup in the host |
| All nine children should ship as one program | Not supported | Treat the epic as a gated roadmap; Track A is independent, Track B is the wedge, and Track C is promoted only by evidence |

#### What already exists

| Sub-problem | Existing code/flow | Reuse decision |
| --- | --- | --- |
| Bounded authoritative execution | `IDurableRuntimePump` and `PostgreSqlDurableRuntimePump` | Keep as the sole pump core; add an opt-in admission result around it |
| Store-backed health and drain | `IDurableRuntimeHealth`, `IDurableRuntimeDrainControl`, `PostgreSqlDurableRuntimeHealth` | Extend the current snapshot and provider implementation |
| Process-local and store admission | `DurableRuntimeAdmissionGate`, `_passGate`, `TryBeginPassAsync` | Project these existing decisions into `IDurableRuntimePumpAdmission`; do not add another lock |
| Work registration and request safety | `DurableWorkRegistration`, `DurableWorkRequest`, codec and registry validation | Build definitions over the same registration builder and request type |
| Schema/epoch CLI safety | `appsurface durable schema status|script|preflight|apply` and `DurableSchemaOnlineCommandBase` | Extend the command family and secret-safe environment pattern |
| Durable diagnostics | `DurableProblemCodes` and existing schema diagnostics | Use one descriptor catalog; do not invent CLI-only meanings |
| Trace source and Flow telemetry | `AppSurfaceActivitySources`, Flow command and resumed-execution activities | Reuse the source; add one activation-service activity rather than using an unrelated Flow command as proof |
| PostgreSQL migration/release proof | Numbered migrations, migration lock, role recipe, package index, packed-consumer verification | Add 0010 and template packaging to these existing gates |
| Downstream proof | Seven Skoolit Durable lanes and endpoint/lifecycle tests | Measure one representative lane first; preserve Skoolit policy outside upstream regions |

#### Dream state and delta

```text
CURRENT
  safe protocol + low-level contracts
        |
        v
  each host repeats identity, health meaning, activation mapping, and fakes

THIS RAIL
  provider-owned assessments
        + typed Work definition
        + deterministic scenario
        + transport-neutral activation
        + doctor/template after gates
        |
        v
  one explicit happy path with host policy still visible

12-MONTH IDEAL
  two or more independent adopters
        |
        +--> only repeatedly proven mechanics promoted
        +--> stable template and diagnostics
        +--> optional adapter only if it removes measured friction
        `--> no broker, deployment controller, or second durable authority
```

The rail reaches the correct middle state. It does not yet prove broad market demand, provider-neutral wake dispatch,
stable ASP.NET adaptation, or a compile-time manifest. Those remain evidence-gated rather than being implied by the
public-preview API.

#### Alternatives revisited

| Approach | Completeness | CC effort | Risk | Decision |
| --- | ---: | ---: | --- | --- |
| Safety patch only: health semantics and docs | 5/10 | <1 day | Fixes the bug but leaves repeated authoring and proof | Reject as the ceiling; Track A may still ship first |
| Competitive wedge: cases 1, 2, 3, and 6 | 9/10 for first adoption | Several bounded cases | Depends on #783; does not yet provide template/doctor polish | Make this the first adoption milestone |
| Entire rail as one release commitment | 10/10 feature coverage | Multi-case | Preview sprawl and coordination failure | Reject; retain the cases as an evidence-gated roadmap |
| Framework-specific host abstraction | 6/10 | Short initially | Moves auth, routes, deployment, or domain policy upstream | Reject at the boundary |

#### Selective-expansion and temporal interrogation

- **Hour 1:** land the operational truth table and `Unavailable` distinction; prove cold-start, stale takeover, drain, and
  caller cancellation.
- **Hours 2–3:** after #783, sketch one typed definition and direct activation path in a real Skoolit lane. Record behavioral
  evidence before freezing the binding API.
- **Hours 4–5:** add deterministic scenarios and the transport-neutral service. The first-success proof must execute real
  Work and export the activation activity.
- **Hour 6+:** start doctor, template, adapter, and target-equivalence work only when each named gate is true. A failed
  experiment closes with evidence; it does not become permanent preview surface.

Mode remains **SELECTIVE EXPANSION**. The complete roadmap is retained, but the release commitment is the smallest
competitive wedge and every convenience surface has an exit gate.

#### CEO dual voices

**CODEX SAYS (CEO — strategy challenge)**

[Running under Codex: the nested Codex CLI pass was skipped by the review skill's anti-recursion guard; this task's own
repository-grounded analysis is the Codex voice.]

- The core problem is real: current Skoolit composition repeats upstream facts and has already produced a readiness bug.
- The roadmap is defensible only if Track C is optional evidence work, line count is not the value verdict, and downstream
  consumers cannot invent their own provider failure taxonomy.
- The current Case 6 exporter test proves Flow command telemetry, not external activation. That would let the rail claim an
  observable activation path without observing it.
- The repository has no template package today. Case 9 must own the missing packaging and isolated install proof.
- A managed competitor can win on setup even with a less explicit safety model. The wedge must be measurably faster without
  hiding the decisions that keep provider effects safe.

**CLAUDE SUBAGENT (CEO — strategic independence)**

- Rated the nine-case program as overbuilt from one downstream adopter and recommended committing only to health, a minimal
  typed definition, and a direct reference host until another adopter succeeds.
- Challenged source-line count as a gameable proxy and asked for time-to-success, explicit-decision, and failure-reproduction
  measures.
- Required an exhaustive provider error taxonomy before doctor, activation, and template layers consume health.
- Warned that established frameworks have a simpler perceived operational model and that package count can increase platform
  tax.
- Recommended publishing a template only after the direct path survives real adoption and separating retention/adapter work
  unless operational evidence makes them urgent.

| Dimension | Claude | Codex | Consensus |
| --- | --- | --- | --- |
| Premises valid? | Partly; one adopter is insufficient | Yes for preview, with a second-adopter stable gate | DISAGREE on confidence |
| Right problem to solve? | Yes, but reduce to a wedge | Yes; the wedge is Track B | CONFIRMED |
| Scope calibration correct? | No; park most children | Yes as a roadmap, no as one release | DISAGREE on roadmap size |
| Alternatives sufficiently explored? | No competitive decision test | Sufficient after adding a measured wedge | DISAGREE |
| Competitive/market risks covered? | No | Managed alternatives lower setup friction | CONFIRMED concern |
| Six-month trajectory sound? | Only with hard promotion gates | Sound with staged commitments and experiment exits | CONFIRMED |

No User Challenge is queued: both voices agree on the first wedge and promotion gates but do not agree that the approved
roadmap itself should be collapsed.

#### Section 1 — Architecture

Two issues were accepted. First, the epic now separates roadmap membership from release commitment. Second, activation
telemetry is emitted by the transport-neutral service so the reference path can prove what it claims. The dependency
direction remains inward: host policy calls Provider contracts, which call the existing pump; PostgreSQL remains authority.

```text
HOST POLICY
  routes + auth + deployment gate + domain preparation/cleanup
        |
        v
IDurableExternalActivationService
  health -> caller/request budget -> admission-aware pump -> bounded outcome
        |             |                    |
        v             v                    v
IDurableRuntimeHealth TimeProvider   IDurableRuntimePumpAdmission
        \____________________________________/
                         |
                         v
          PostgreSQL provider and authoritative store

Typed definition -> existing registration builder -> registry -> pump execution
Typed definition -> existing DurableWorkRequest -> acceptance -> PostgreSQL truth
```

```text
                    +------------ unavailable -----------+
                    |                                      |
startup/no row -> NotStarted -> Healthy -> Stale ----------+--> observed health only
                    |          |        |
                    |          |        +--> guarded provider takeover -> Healthy
                    |          +--> BeginDrain -> Draining -> Resume -> NotStarted/Healthy
                    |
schema/epoch mismatch -------------------------------> Incompatible

No health-state transition authorizes effects. Admission and the PostgreSQL permit remain authoritative.
```

#### Section 2 — Error and rescue map

The accepted change distinguishes unexpected failure before and after `pumpStarted`; `ActivationFailed` no longer lies that
the pump ran. Health and admission share one provider failure classifier.

##### Error & Rescue Registry

| Method/codepath | Failure | Classification/rescue | Builder/operator sees |
| --- | --- | --- | --- |
| `DurableRuntimeHealthSnapshot` construction | Invalid enum, identifier, count, or contradictory facts | Argument failure; never publish snapshot | Exact invalid contract member |
| `IDurableRuntimeHealth.GetAsync` | Caller canceled | Propagate `OperationCanceledException` | Request/deployment cancellation |
| `IDurableRuntimeHealth.GetAsync` | Connection, permission/provider, or deadline failure | `Unavailable` + `ASDUR103` | Dependency unavailable; retry/check grants |
| `IDurableRuntimeHealth.GetAsync` | Observed schema/epoch mismatch | `Incompatible` + specific existing code | Exact migration/epoch remediation |
| `IDurableRuntimeHealth.GetAsync` | Malformed provider result/programming defect | Propagate and log at host boundary | Failure is not relabeled as store state |
| `IDurableRuntimePumpAdmission.TryRunOnceAsync` | Overlap, closed gate, drain, or guarded takeover refusal | `Refused` | `Busy`; no processed-work claim |
| `IDurableRuntimePumpAdmission.TryRunOnceAsync` | Store unavailable | `Unavailable` + `ASDUR103` | Safe retry decision |
| `IDurableRuntimePumpAdmission.TryRunOnceAsync` | Schema/epoch race | `Incompatible` + specific code | Re-run deployment preflight |
| Definition/binding construction | Invalid identity, codec, safety, or incomplete reconciler/exit binding | Argument/invalid-operation failure | Exact binding rule |
| `CreateRequest` | Invalid per-request facts or codec failure | Existing request/codec exception with definition context | Request is not accepted |
| `ActivateAsync` health phase | Known unavailable/incompatible/draining state | Closed canonical outcome | Stable mapper input |
| `ActivateAsync` before `pumpStarted` | Caller/budget cancellation | `CanceledBeforeAdmission` or `RequestBudgetExceeded` | Safe no-pump result |
| `ActivateAsync` before `pumpStarted` | Unexpected nonfatal dependency failure | `ActivationFailed` + `ASDUR407`; structured safe log | Generic safe activation failure |
| `ActivateAsync` after `pumpStarted` | Cancellation | `PumpCanceled`; no effect-truth inference | Inspect durable record/#783 exit |
| `ActivateAsync` after `pumpStarted` | Unexpected nonfatal pump failure | `PumpFailed` + bounded code | Safe failure; inspect durable state |
| Heartbeat maintenance | Advisory lock unavailable | Skip maintenance | No correctness impact |
| Heartbeat maintenance | Timeout/provider failure | Roll back batch; record bounded outcome | Capacity warning; pump remains authoritative |
| Doctor | Invalid input | Exit 3 | Problem/cause/fix without DB mutation |
| Doctor | Store unavailable | Exit 4 | Safe connection/grant guidance |
| Template startup | Missing production auth scheme or Development token | Fail startup | Exact configuration action |

#### Section 3 — Security and threat model

No boundary expansion was accepted. Routes, authentication, authorization, wake transport, and domain materialization remain
host-owned. The template's development token stays opt-in, random per run, fixed-time compared, and impossible outside
Development; doctor and health outputs remain bounded and payload-free. Case 4's security-definer function retains exact
signature grants and no direct runtime deletion.

#### Section 4 — Data flow and interaction edges

The plan already covers nil/empty/invalid request facts, cold start, stale takeover, drain, overlap, cancellation races,
schema/epoch races, and unexpected failures. The missing edge was measurement gaming: a smaller mapping that hides lifecycle
decisions now fails the behavioral evidence even if it passes the line-count guardrail.

```text
request
  |
  +-- null/invalid pump or budget ----------> constructor failure, no side effect
  |
  v
health observation
  +-- unavailable/incompatible/draining ----> closed outcome, no pump
  +-- canceled -----------------------------> canceled-before-admission
  +-- unexpected ---------------------------> activation-failed
  |
  v
admission
  +-- refused ------------------------------> busy
  +-- store/schema race --------------------> pump-failed, no effect inference
  |
  v
pumpStarted -> bounded pump
  +-- empty result --------------------------> completed, exact zero counts
  +-- has more ------------------------------> completed, exact `HasMore`
  +-- canceled/fails ------------------------> pump-canceled/pump-failed
```

#### Section 5 — Code quality

One DRY correction was accepted: health and admission-aware pump translation use one internal PostgreSQL failure classifier.
Definitions continue to reuse the existing registration builder, request type, registry, codecs, and lifetimes. No assembly
scan, reflection registry, transport abstraction, or policy callback was added.

#### Section 6 — Tests

The test strategy now proves behavioral adoption evidence and the real activation activity. Existing exhaustive contract,
PostgreSQL, packed-consumer, public-API, role, and CLI patterns are reused. Host-specific endpoint tests remain responsible
for authorization and response mapping; provider tests remain responsible for store truth and races.

#### Section 7 — Performance

No new synchronous hot-path work was accepted beyond one low-cardinality activity and existing bounded health/admission
queries. Heartbeat cleanup remains opportunistic, lock-skipping, batched, and correctness-independent; its public defaults
remain blocked on the named 100,000-row/deployment-churn evidence.

#### Section 8 — Observability

The Flow-command proxy was removed from the adoption claim. Case 6 now emits and exports one activation-service activity with
safe fixed tags. Native Work claim/execution spans remain explicitly separate; the plan does not claim they exist.

#### Section 9 — Deployment and rollback

Case 9 now owns a real template pack project and isolated install proof because no template package exists. Existing NuGet
publication remains the distribution channel. Additive APIs roll back by reverting consumers first; migration 0010 rolls
back operationally by disabling maintenance and reverting provider calls while leaving the safe function/index installed.

```text
1 schema script/review -> 2 migration + role recipe -> 3 package/provider
        -> 4 reference host -> 5 adopter proof -> 6 convenience promotion

Failure?
  API only ----------> disable new composition / revert consumer / retain old overloads
  activation service -> route stays host-disabled / revert service registration
  retention ---------> disable cadence / revert caller / leave compatible function+index
  template ----------> unlist preview package / keep prior stable template available
```

#### Section 10 — Long-term trajectory

Reversibility is 4/5 because public-preview APIs are additive, experiments have deletion gates, migrations are forward-only,
and stable template/adapter promotion requires distinct adoption. The remaining risk is preview-surface accumulation; the
tracking case must close failed experiments rather than preserving them indefinitely.

#### Section 11 — Design

Skipped. The plan has no user-interface surface; dashboard work is an explicit non-goal.

#### Failure Modes Registry

| Codepath | Failure mode | Rescued? | Test? | User sees | Logged? |
| --- | --- | ---: | ---: | --- | ---: |
| Health observation | Database cannot be observed | Yes | Yes | `Unavailable`/`ASDUR103` | Yes |
| Health observation | Schema or epoch mismatch | Yes | Yes | `Incompatible` + specific code | Yes |
| Health observation | Malformed provider contract | No, propagates | Yes | Host failure, not false health | Yes |
| Pump admission | Concurrent/closed/draining pass | Yes | Yes | `Busy` | Optional low-cardinality |
| Pump admission | Store/schema race | Yes | Yes | `PumpFailed` + safe code | Yes |
| Activation | Caller and deadline race before pump | Yes | Yes | Deterministic cancellation outcome | Yes |
| Activation | Cancellation after pump starts | Yes | Yes | `PumpCanceled`; inspect durable truth | Yes |
| Activation | Unexpected pre-pump failure | Yes | Yes | `ActivationFailed`/`ASDUR407` | Yes |
| Activation | Unexpected post-start failure | Yes | Yes | `PumpFailed`/`ASDUR407` | Yes |
| Definition/request | Repeated or contradictory contract facts | Yes, fail fast | Yes | Exact contract diagnostic | N/A |
| Heartbeat maintenance | Lock held or rows concurrently renewed | Yes, skip/recheck | Yes | No correctness change | Bounded result |
| Heartbeat maintenance | Batch times out | Yes, rollback | Yes | Capacity diagnostic | Yes |
| Doctor | Missing/invalid env or role | Yes | Yes | Exit 3 + cause/fix/docs | No secrets |
| Doctor | Dependency unavailable | Yes | Yes | Exit 4 + safe guidance | No secrets |
| Template activation | Missing/wrong auth | Yes | Yes | 401/403 or startup failure | Host policy |

No row is both unrescued, untested, and silent.

#### NOT in scope

- Broker ownership, provider-neutral wake dispatch, queue correctness, autoscaling policy, or a deployment controller.
- Application routes, authentication scheme choice, authorization claims, outbox/materialization, reconciliation, cleanup,
  or domain continuation.
- A dashboard or management service.
- Native Work claim/execution spans; activation-service telemetry does not imply them.
- A compile-time Work manifest or CLI loading/executing consumer assemblies.
- Stable target-equivalence or ASP.NET adapter APIs without their two-consumer/adopter evidence.
- Replacing #783's terminal exit contract or #765's persisted retry/deadline policy.

#### Stale diagram audit

No touched runtime source file contains a competing architecture diagram. The pump's existing comments still accurately state
that PostgreSQL owns authoritative decisions and that the pump core itself is not the owner of the new activation-service
span.

#### Implementation Tasks — CEO synthesis

- [ ] **CEO-T1 (P1, human: ~2d / CC: ~30min)** — Operational contracts — Implement the shared health/admission failure
  classifier and exhaustive state/exception matrix.
  - Surfaced by: Error map and Code Quality.
  - Files: Provider health contracts, PostgreSQL health/pump, tests, public API snapshots, package docs.
  - Verify: focused Durable Provider/PostgreSQL tests plus `Durable/verify-postgresql.sh`.
- [ ] **CEO-T2 (P1, human: ~1d / CC: ~20min)** — Adoption evidence — Record behavior-first Skoolit baseline/proposed
  evidence and retain line count as a secondary guardrail.
  - Surfaced by: Premise challenge and Data Flow.
  - Files: `Durable/evidence/*` and adoption metrics tool/tests.
  - Verify: metrics result is deterministic and all behavior assertions are evidenced.
- [ ] **CEO-T3 (P1, human: ~1d / CC: ~20min)** — Activation — Emit and export
  `appsurface.durable.runtime.activation` through the real reference-host path.
  - Surfaced by: Architecture and Observability.
  - Files: external activation service, reference host/tests, Observability docs.
  - Verify: in-memory exporter receives the safe-tag activity after authenticated activation.
- [ ] **CEO-T4 (P2, human: ~2d / CC: ~30min)** — Template distribution — Add an explicit template pack project and
  isolated packed-artifact install/create/test gates.
  - Surfaced by: Deployment and six-month trajectory.
  - Files: new template project/manifest, solution, package index, package/release workflows, docs.
  - Verify: install the produced `.nupkg` into an isolated CLI home and run the generated proof.

#### CEO completion summary

| Review area | Result |
| --- | --- |
| Mode | SELECTIVE EXPANSION |
| System audit | Existing health, pump, registration, CLI, migration, telemetry, and package paths mapped |
| Premise decisions | 6 assessed; 4 accepted with gates, 1 corrected, 1 rejected |
| Architecture | 2 issues accepted |
| Error paths | 20 grouped paths mapped; 0 critical gaps after corrections |
| Security | 0 new unresolved issues |
| Data/edge cases | 1 measurement gap corrected |
| Code quality | 1 DRY correction accepted |
| Tests | 2 proof gaps corrected |
| Performance | 0 unresolved issues; retention defaults remain evidence-gated |
| Observability | 1 false proxy replaced with direct activation evidence |
| Deployment | 2 risks corrected |
| Future | Reversibility 4/5; preview accumulation remains the watch item |
| Design | SKIPPED, no UI scope |
| NOT in scope | Written |
| Dream-state delta | Written |
| Diagrams | Architecture, data/error, deployment, rollback, and health-state intent |
| Unresolved decisions | 0 |

**Phase 1 complete.** Codex: 5 concerns. Claude subagent: 5 issues. Consensus: 3/6 confirmed, 3 disagreements
resolved by keeping the roadmap but hardening its promotion gates. Passing to Phase 2.

<!-- AUTONOMOUS DECISION LOG -->
## Decision Audit Trail

| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | CEO | Keep the nine-child rail as a gated roadmap, but make Track B the first adoption milestone | Scope | P3 Pragmatic | Preserves independently valuable work without pretending every child is one release commitment | Collapse all work into three cases; ship all nine together |
| 2 | CEO | Require a distinct adopter before stable convenience-surface promotion | Evidence gate | P6 Bias to action | Skoolit proves repetition; a second adopter proves generality | Treat seven lanes in one app as market-wide proof |
| 3 | CEO | Make behavior-first measures primary and source-line count secondary | Correctness | P1 Completeness | Time, explicit decisions, reproducible failures, and duplicated facts measure adoption better than formatting | Use line count as the sole pass/fail value signal |
| 4 | CEO | Add one shared provider failure classifier before downstream layers consume health/admission | Architecture | P4 DRY | Prevents doctor, service, template, and hosts from drifting on unavailable vs incompatible semantics | Let each caller classify exceptions |
| 5 | CEO | Distinguish `ActivationFailed` before pump start from `PumpFailed` afterward | API semantics | P5 Explicit over clever | The outcome tells builders whether the pump was invoked without inferring durable effect truth | Label every unexpected failure `PumpFailed` |
| 6 | CEO | Prove the activation path with an activation-service activity | Observability | P1 Completeness | A separate Flow command cannot prove that external activation is observable | Keep the unrelated Flow activity as the rail's exporter proof |
| 7 | CEO | Make Case 9 own template packaging and isolated installation | Distribution | P1 Completeness | The repository has no existing template package; distribution must be part of the feature | Assume template packaging already exists |
| 8 | CEO | Keep heartbeat retention independent and evidence-gate its public defaults | Scope | P2 Boil lakes | Storage is genuinely unbounded, but cleanup policy must be proven separately from adoption | Drop retention as unrelated; let it block Track B |
